# Merge Plan — grok-install-action
This repository will remain standalone as the official CI gate for Grok-Native certification.
Merge to the main branch. It will continue to be the canonical GitHub Action that validates .grok-install.yaml files.
